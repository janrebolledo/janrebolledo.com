---
const { list } = Astro.props;
---

{
  list.length > 0 ? (
    list.map((item: string, index: number) => (
      <li>
        {item}
        {index < list.length - 1 && " •"}
      </li>
    ))
  ) : (
    <li>{list[0]}</li>
  )
}
